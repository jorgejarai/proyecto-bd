mutation AddPerson(
  $rut: Int
  $name: String!
  $division: String
  $address: Int
  $email: String
  $phone: Int
) {
  addPerson(
    rut: $rut
    name: $name
    division: $division
    address: $address
    email: $email
    phone: $phone
  ) {
    status
    person {
      id
      rut
      name
      division
      phone
      email
    }
    address {
      id
      address
      postalCode
      country {
        name
      }
    }
  }
}
