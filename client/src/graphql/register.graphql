mutation Register(
  $email: String!
  $username: String!
  $name: String!
  $password: String!
) {
  register(
    email: $email
    username: $username
    name: $name
    password: $password
  ) {
    status
    message
  }
}
