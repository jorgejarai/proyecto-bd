query Person($id: Int!) {
  person(id: $id) {
    person {
      id
      name
      rut
      division
      phone
      email
    }
    address {
      address
      postalCode
      country {
        countryNumber
        name
      }
    }
  }
}
