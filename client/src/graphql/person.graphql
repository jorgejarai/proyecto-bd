query Person($id: Int!) {
  person(id: $id) {
    status {
      status
      message
    }
    person {
      id
      name
      rut
      division
      phone
      email
      address {
        id
        address
        postalCode
        country {
          countryNumber
          name
        }
      }
    }
  }
}
